const detailLevelEnum = Object.freeze({
  medium: 'medium',
  detail: 'detail',
});

const formInputsEnum = Object.freeze({
  startDate: 'startDate',
  endDate: 'endDate',
  searchTerm: 'SearchTerm'
});

export default { detailLevelEnum, formInputsEnum };
